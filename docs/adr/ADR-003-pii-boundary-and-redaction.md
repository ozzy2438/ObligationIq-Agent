# ADR-003: PII boundary and redaction

## Context

Names, addresses and NMIs must not reach a model. Every request invokes the redaction interface.

## Decision

PENDING — detailed design is deferred to its build phase.

## Consequences

The Phase 0 stub blocks live dispatch. Detection, stable pseudonyms, rehydration and security validation are pending.
